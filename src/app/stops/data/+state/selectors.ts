import { createFeatureSelector } from '@ngrx/store';
import { StopsState, stopsFeatureKey } from './reducers';

// export const selectStopsState =
//   createFeatureSelector<StopsState>(stopsFeatureKey);
